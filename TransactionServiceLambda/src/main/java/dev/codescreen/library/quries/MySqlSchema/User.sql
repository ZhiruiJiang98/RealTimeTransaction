CREATE TABLE User
(
    id         VARCHAR(255),
    userName   VARCHAR(255),
    email      VARCHAR(255),
    password   VARCHAR(255),
    createdTime VARCHAR(255),
    updatedTime VARCHAR(255),
    PRIMARY KEY (id)
);